# get pkgs
sudo pacman -S awesome base-devel git atril caja mate-calc alacritty arandr htop bitwarden dmenu cmatrix feh ffmpeg gimp gparted python

# install yay
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si
cd ~

# get pkgs (aur) 
yay -S brave-browser github-desktop

# get pkgs (pip)
pip install markdown
